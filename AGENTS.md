# sirumo配下の独立サービス向け共通開発ガイド

このファイルは、sirumoの親ドメイン配下で独立運営する各サービスのリポジトリで共通利用する開発方針です。特定のサービス名、ドメインのTLD、フレームワークを前提にしません。

## 目的と優先順位

- 検索・比較・計算によって、利用者の具体的な困りごとを解決する小規模なサービスを作る。
- 個人で維持できる運営負担に抑え、検索流入と収益を少しずつ育てる。
- 優先順位は、情報と処理の正確性、使いやすさ、維持費、保守性、検索での見つけやすさ、収益化の順とする。
- AIは開発・調査・更新を支援するために使う。記事数やページ数を増やすこと自体を目的にしない。
- 検索需要、競合の強さ、広告収益、検索順位を根拠なく断定しない。事実・仮説・試算を区別する。

## プロジェクトの前提

- 親ドメインの名称はsirumo。利用者向けの共通ブランドとしては使わず、各サービスを独立した名称・目的・導線で運営する。サービスごとにリポジトリと公開プロジェクトを分け、取得済み親ドメインのサブドメインで展開する。
- 実際のドメイン、表示名、Cloudflareリソース名はリポジトリ内の設定を確認する。未確認の値を本番設定に書かない。
- 収益が安定するまでは、独自ドメイン代を除き無料枠内での運用を基本とする。
- ホスティングはCloudflareを前提とし、PagesまたはWorkersなどの具体的な構成は既存設定と要件に合わせる。
- 開発には契約済みのCodex・Claude Codeを利用する。開発ツールの契約にAPI利用料が含まれるとは仮定しない。
- 公開サービスに有料AI API、常時稼働サーバー、有料データソースを必須にしない。
- サービス固有の目的、対象範囲、URL、実行コマンド、データ更新方法はREADMEに記録する。この共通ファイルに他サービスの実装詳細を混ぜない。

## 作業の進め方

- 思考・内部検討は英語で行い、ユーザー向けの説明、進捗、質問、完了報告、作成するドキュメントは日本語で記述する。コードの識別子、コマンド、固有名詞などは必要な原語表記を維持する。
- 最初にREADME、既存コード、パッケージ定義、Cloudflare設定、関連する指示ファイルを確認する。
- 既存の実装とユーザーの変更を尊重し、依頼に不要な全面改修や技術スタックの変更をしない。
- ユーザーが依頼した範囲の実装・調査・修正・検証は進める。通常の実装判断について逐一確認を求めない。
- 技術・設計・実装方法の選択では、自身の知識、推測、思い込みだけで決めない。既存実装、公式ドキュメント、一次情報を調査し、複数の現実的な選択肢を挙げて比較・選定する。
- 選択肢は要件への適合、正確性、無料運用、保守性、実装負担、既存構成との一貫性で比較する。候補を並べてユーザーに判断を委ねず、調査結果に基づいて最適な案を自律的に選び、実装・検証まで進める。
- 重要な選択は、比較した案、採用理由、主なトレードオフを日本語で簡潔に記録する。調査で確認できなかった点を確認済みとして扱わない。調査の深さは変更の影響に合わせる。
- 不明点があっても、独立して進められる作業を進める。公開情報の正確性や費用に影響する不明点を推測で埋めない。
- 費用の発生、データ削除、本番の破壊的変更、外部への連絡は、既存の依頼に含まれるか確認する。含まれない場合だけ事前に確認する。
- 本番公開はユーザーの依頼と既存の公開方針に従う。承認が必要な場合は、実装・検証・確認可能な差分を用意してから求める。
- 完了時は、変更点、検証結果、未確認事項を日本語で簡潔に報告する。

## 構成と無料運用

- 静的配信とブラウザー内処理を優先する。サーバー処理やデータベースは必要性がある場合に追加する。
- 小規模な構造化データは、まずGit管理のJSON等で扱えるか検討する。データ量・更新方法に合わなくなったら移行する。
- 料金・無料枠・API制限など変化する条件は、採用時に公式情報で確認する。「永久無料」や「無制限」と推測しない。
- リクエスト数、ビルド回数、ストレージ、外部API呼び出しが増え続ける構成を避ける。必要に応じてキャッシュ、実行間隔、処理上限を設ける。
- 更新取得に失敗した場合は、最後に検証済みのデータを維持し、失敗を記録する。空データで上書きしない。
- 秘密情報はリポジトリやクライアントコードに含めない。環境変数のサンプルにはキー名と説明だけを載せる。
- 複数サービスで再利用できる設計は歓迎するが、実際の重複がない段階で共通パッケージや大きな基盤を作らない。

## データの信頼性

- 原則として公式サイト、公式資料、提供条件が明確なオープンデータを一次情報にする。
- 外部情報に基づくレコードには、安定したID、出典URL、確認日を持たせる。必要なら対象地域、型番、適用期間、データ版も記録する。
- 情報源の更新日、取得日、人が内容を確認した日を混同しない。自動取得だけで「確認済み」としない。
- 不明・未確認・対象外・なしを区別する。情報が見つからないことを「設備なし」「対応不可」と解釈しない。
- AIが推測した設備、エラーコード、対応機種、自治体ルール、価格、口コミを事実として公開しない。
- 公式情報に基づく説明と、独自の計算・推定・検証結果を区別して表示する。
- 取得前に利用条件、ライセンス、アクセス制限を確認する。大量取得や第三者の文章・画像・データベースの転載を無条件に行わない。
- 自動取得は低頻度・差分取得を基本にし、再試行回数を制限する。アクセス制限や認証を回避しない。
- 公開データと検証用データを分け、架空のサンプルを実データとして検索対象に含めない。

## サービス別に適用する確認事項

該当するサービスの項目だけを適用する。

- SA/PA：路線、上下線、施設の位置、設備、設備ごとの営業時間を区別する。同名施設や反対車線の設備を混同しない。未確認のリアルタイム営業状況を表示しない。
- 家電エラー：メーカー、製品カテゴリ、型番、コード表記を組み合わせて管理する。同じコードを別機種に流用しない。対処は公式の利用者向け手順に基づき、危険な分解や安全機能の解除を勧めない。
- 計算・変換：式、単位、入力範囲、丸め規則、前提を明記する。ゼロ、負数、空入力、桁あふれ、境界値を扱う。使用できない入力を黙って補正してもっともらしい値を返さない。
- ゴミ分別：自治体と必要な地区、品目、サイズ・材質等の条件、手数料、申込先、適用時期を区別する。別自治体のルールを一般化しない。

## Design

UIを新規作成・大幅変更する場合は、実装前に以下を行うこと。

1. https://styles.refero.design/ から、対象サービスの用途・情報量・ユーザー層に適したデザインを探す
2. 単に見た目が派手なものではなく、可読性・操作性・情報設計を優先する
3. 選んだデザインの DESIGN.md を参考にして、以下を統一する
   - Typography
   - Color
   - Spacing
   - Border radius
   - Shadows
   - Components
4. 元サイトをそのままコピーせず、プロダクトの用途に合わせて調整する
5. 既存画面がある場合は、既存デザインとの一貫性を優先する

## UIとアクセシビリティ

- スマートフォンで検索・入力・結果確認が完結する設計にする。
- 主機能をページ上部に置き、検索エンジン向けの長文や広告で操作を妨げない。
- 入力欄にラベルを付け、キーボード操作、フォーカス表示、十分なコントラストを確保する。
- 読み込み中、結果なし、入力エラー、データ未確認、取得失敗を区別し、利用者が次にできる操作を示す。
- 出典、確認日、適用条件など、判断に必要な情報を結果の近くに表示する。
- サービス固有の名称・ロゴを使う。sirumoの共通ロゴ、「姉妹サイト」「関連サービス」一覧、親ドメインへの誘導、サービス横断ナビゲーションは追加しない。実装技術やAIの内部処理は、利用者の判断に必要な場合以外は画面に出さない。

## サービスの独立性と検索対象の分担

- サービスごとに対象利用者、解決する課題、扱うテーマと扱わないテーマをREADMEに記録する。
- 同じ検索意図に答える実質同一のページや機能を、複数サービスに重複して公開しない。新機能が他サービスの対象と重なる場合は、どちらで扱うか整理する。
- 特に汎用の計算・変換サービスは、専門サービスが扱う計算機能との重複を確認する。共通ロジックの再利用と、検索流入用ページの重複公開は区別する。
- 本文・タイトル・説明文・構造化データのサイト名は当該サービスに合わせる。WebSite構造化データを使う場合は各サブドメインのトップページで固有の名称とURLを指定する。
- canonical、サイトマップ、robots.txtは各サービス単位で管理する。独立した内容のページを親ドメインや別サービスにcanonical指定しない。
- 検索順位操作を目的とするサービス間の相互リンクは設置しない。各サービス内で利用者の目的が完結する導線を作る。
- 計測はサービス単位で識別できるようにする。Search Consoleでは必要に応じてサブドメインのURLプレフィックス単位で確認する。
- 同じ親ドメインの利用は公開URLから分かるため、運営上の関連が完全に隠れる、または検索評価が完全に分離されるとは説明しない。
- 運営者情報やプライバシー表示は実態に基づく。サービスを独立して見せるために架空の運営者を記載しない。

## SEOとページ設計

- 検索意図ごとに、実際に役立つ情報や機能を備えたページを作る。
- 主要な説明、データ、内部リンクは可能な限り配信HTMLで読めるようにする。検索流入用ページの内容をクライアント処理だけに依存させない。
- タイトル、見出し、説明文はページ内容に一致させ、対象や条件の違いが分かるものにする。
- 本番URLを基準にcanonical、サイトマップ、内部リンクを整合させる。プレビューURLを本番サイトマップに混ぜない。
- パラメーターや絞り込みの全組み合わせをインデックス対象にしない。検索需要と独自の価値があるものだけ公開ページとして設計する。
- 空の検索結果、重複ページ、未完成ページを量産しない。存在しないページには適切なHTTPステータスを返す。
- 構造化データは、画面上の実際の内容と適用条件に合う場合だけ使う。架空の評価やレビューを追加しない。
- 中身を更新していないページの更新日を自動的に書き換えない。
- SEO指標の改善だけを理由に、正確性や操作性を損なう変更をしない。

## GEO：AI検索で参照しやすい情報設計

- GEO（生成AI検索向けの最適化）も設計対象とする。AIによる引用・推薦・送客は保証せず、確認できた効果と仮説を区別する。
- 各ページで、何についての回答か、結論、適用条件、根拠、確認日を明確にする。見出しや表を使い、該当箇所だけ読んでも意味が通じるようにする。
- 施設名・路線・上下線、メーカー・型番、自治体、単位などの対象を省略しすぎず、同名・類似の対象を識別できるようにする。
- 公式情報の言い換えだけにせず、条件別の比較、横断検索、検証可能な計算、独自検証などの価値を提供する。
- 重要な事実と出典を対応付ける。推計値には計算方法と前提を添え、AIが条件を落として引用しても誤解されにくい表現を心がける。
- 参照先になる安定したURLを設ける。利用者とクローラーに異なる事実や誘導文を見せず、AIへの指示をコンテンツに埋め込まない。
- AI検索向けの特別なマークアップやllms.txtを、掲載条件や効果が保証された施策として扱わない。追加する場合は公式の対応状況と保守負担を確認する。
- AI経由の流入は識別できる範囲で計測する。リファラーだけで全流入・全引用を把握できるとは考えない。

## robots.txtとクロール制御

- 共通の公開要件として、各サービスのホスト直下に `/robots.txt` を必ず配信する。検索エンジン全般がファイルの存在を必須としているという意味ではない。
- 親ドメインのrobots.txtは各サブドメインには適用されない。サービスごとに管理・検証する。
- 公開コンテンツは検索クローラーが取得できる設定を基本とし、ページ表示に必要なCSS・JavaScript等を不用意に遮断しない。
- `Sitemap:` に当該サービスの本番サイトマップの絶対URLを記載する。サイトマップには正規の公開URLだけを含める。
- robots.txtはクロール制御であり、アクセス制御でも確実なインデックス削除手段でもない。秘密情報や管理画面は認証等で保護する。
- インデックス除外には適切なmeta robotsまたはX-Robots-Tagを使う。robots.txtに `noindex` を書かない。除外指示を読み取らせる必要のあるURLをDisallowで遮断しない。
- プレビュー環境には公開方針に応じて認証またはnoindexを設定する。本番にプレビュー用の全拒否・noindexが混入しないよう確認する。
- AI検索用、学習用、ユーザー操作による取得用のボットを混同しない。名称・役割・制御方法を各提供者の最新公式資料で確認する。検索露出を目的とするボットを一括して遮断しない。
- 学習利用の許可方針は検索露出とは別に扱い、既存方針を尊重する。特定のボットを許可すれば学習・引用・掲載が保証されるとは説明しない。
- Cloudflare側のWAF・Bot制御・マネージド設定も確認し、robots.txtで許可していても必要な取得が遮断されていないか検証する。
- 初回公開と関連設定の変更時は、robots.txtがHTTP 200・テキストとして取得できること、サイトマップのURL、主要ページのクロール可否とnoindexの有無を確認する。
- 仕様を確認する際の一次情報：
  - Google AI機能：https://developers.google.com/search/docs/appearance/ai-features
  - robots.txt：https://developers.google.com/search/docs/crawling-indexing/robots/intro
  - noindex：https://developers.google.com/search/docs/crawling-indexing/block-indexing

## 計測とAIによる継続改善

- Search Console等の利用可能なデータから、検索語、表示回数、クリック、対象ページを確認する。未接続の計測を接続済みと扱わない。
- 順位だけでなく、検索・計算の完了、外部リンクのクリック、確定報酬など、サービスの目的に近い指標を選ぶ。
- 必要以上の個人情報や検索入力を収集しない。アクセス解析や広告を追加するときは実態に合う説明を用意する。
- 自動改善は、データ収集、問題抽出、修正案、検証、公開、効果確認を分け、変更を追跡できるようにする。
- 初期の自動修正は、検証可能で影響が限定されたものを対象とする。本文の意味、対処方法、計算式、料金、推奨順位の変更は追加の根拠確認を行う。
- AIに出典のない説明を補完させたり、計測値の短期変動だけで大量のページを書き換えたりしない。
- 改善ごとに仮説、対象、変更内容、実施日を記録する。十分なデータを待ち、季節性や外部要因も考慮して評価する。
- 公開変更はGit等で履歴を残し、問題があれば戻せるようにする。

## 収益化

- 利用者の課題に関係する商品・サービスだけを紹介する。報酬額だけで推薦や検索結果の順序を変えない。
- アフィリエイトの提携可否、成果条件、報酬率は確認した情報に基づく。未提携のリンクを有効な成果リンクとして扱わない。
- 広告・アフィリエイトであることを利用者に分かるように表示し、リンク属性は媒体・検索エンジンの要件に合わせる。
- 広告を検索結果や操作ボタンと紛らわしく配置しない。広告によるレイアウト移動を抑える。
- 広告導入前でも、主機能だけで役立つサービスとして成立させる。

## 検証と保守

- 既存の実行環境とパッケージマネージャーを使い、実際に存在するコマンドで検証する。
- 変更範囲に応じてビルド、型検査、静的解析、関連テストを実施する。実行できなかった検証は理由とともに報告する。
- 計算式、単位変換、データ照合、検索条件、更新処理には重要な境界条件を確認するテストを設ける。計算の期待値は独立した式や既知の例で確認する。
- データには必須項目、ID重複、形式、値域、参照整合性の検証を用意する。
- UI変更は主要操作とモバイル表示を確認する。軽微な文言・装飾変更に不要なテスト基盤を追加しない。
- 新しい依存関係は必要最小限にし、Cloudflareでの互換性とクライアント配信量を考慮する。
- セットアップ、ローカル実行、検証、デプロイ、データ更新の手順が変わったらREADMEも更新する。
